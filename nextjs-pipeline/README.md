# on the todo, 

to run this,
this works for next.js projects that have TS available. 

process to get it started: 
- install prettier as --save-dev
- chmod the two hook files
- configure git to target hooks folder instead of default. 




what must be done manually, 

- installing prettier as --save-dev
- chmod the files
- instruct git to use the hooks folder. 

the goal is to make a single script that automates this entire thing. the one thing that you should be doing is 
~~~
sudo chmod +x <config-script>
~~~
and then clicking that script to setup everything necessary.
that is on the next todo. use the nextjssandbox to practice all this.


what I can add is also checkers too... 
what if I added functions that like... check if someone isn't installed, then we install it? that would be nice, no? more effectively, we could just put that in the setup script, y'know?



what would be wild automation is if we made this into an entire directory what kind of project it is and configured it as so. 
like ruby on rails. 
like next.js
or express.js.
that type of thing.
it would be like... 
check languages available
check framework? 
check... etc. 
and when there's something liek that, it can kinda set it up, no?
